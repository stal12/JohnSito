import { UnderPressureDirective } from './under-pressure.directive';

describe('UnderPressureDirective', () => {
  it('should create an instance', () => {
    const directive = new UnderPressureDirective();
    expect(directive).toBeTruthy();
  });
});
